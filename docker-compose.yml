services:
  nginx:
    image: nginx:alpine
    container_name: kub-ng-1-nginx
    ports:
      - "1080:80"
    volumes:
      - ./dist/kub-ng-1/browser:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
